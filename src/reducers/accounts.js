import { initalState } from './initial-state'

export default function accounts(state = initalState, action) {
  // switch (action.type) {
  //   case 'INCREMENT':
  //     return state + 1
  //   case 'DECREMENT':
  //     return state - 1
  //   default:
      return state
  // }
}
